This is just an application that build to learn how the LLM API is connected to application like chatbot

this application use GROQCLOUD api as of now the limits are high so i prefer this, and also i tried the gemini-api but the limit is over error occurs so i switched this 
